.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install toolbox

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv toolbox
    $ pip install toolbox
